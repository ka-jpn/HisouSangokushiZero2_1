<?xml version="1.0" ?>
<UserControl 
  x:Class="HisouSangokushiZero2_1_Uno.Pages.PersonList"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:local="using:HisouSangokushiZero2_1_Uno.Pages">
  <UserControl.Resources>
    <DataTemplate x:Key="PersonItemsRepeater" x:DataType="local:PersonListData">
      <Grid Background="{x:Bind Brush}" HorizontalAlignment="Stretch" MinHeight="25" Padding="0,0,0,1">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="50"/>
          <ColumnDefinition Width="80"/>
          <ColumnDefinition Width="50"/>
          <ColumnDefinition Width="50"/>
          <ColumnDefinition Width="40"/>
          <ColumnDefinition Width="40"/>
          <ColumnDefinition Width="40"/>
          <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Column="0" Text="{x:Bind Country}" TextAlignment="Center" VerticalAlignment="Center"/>
        <TextBlock Grid.Column="1" Text="{x:Bind Name}" TextAlignment="Center" VerticalAlignment="Center"/>
        <StackPanel Grid.Column="2" Orientation="Horizontal">
          <TextBlock Text="{x:Bind Role}" VerticalAlignment="Center"/>
          <Image Source="{x:Bind RoleImage}" Width="16" Height="16" VerticalAlignment="Center" x:Phase="1"/>
        </StackPanel>
        <TextBlock Grid.Column="3" Text="{x:Bind Rank}" TextAlignment="Center" VerticalAlignment="Center"/>
        <TextBlock Grid.Column="4" Text="{x:Bind BirthYear}" TextAlignment="Center" VerticalAlignment="Center"/>
        <TextBlock Grid.Column="5" Text="{x:Bind AppearYear}"  TextAlignment="Center" VerticalAlignment="Center"/>
        <TextBlock Grid.Column="6" Text="{x:Bind DeathYear}" TextAlignment="Center" VerticalAlignment="Center"/>
        <TextBlock Grid.Column="7" Text="{x:Bind Biography}" Padding="5,0,0,0" TextAlignment="Start" VerticalAlignment="Center"/>
      </Grid>
    </DataTemplate>
  </UserControl.Resources>
  <Grid x:Name="ContentPanel" Padding="2">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition/>
    </Grid.RowDefinitions>
    <StackPanel Grid.Row="0">
      <StackPanel HorizontalAlignment="Center" Orientation="Horizontal">
        <TextBlock Text="表示シナリオ：" VerticalAlignment="Center"/>
        <ComboBox Width="200" Height="40" x:Name="ScenarioComboBox"/>
      </StackPanel>
      <StackPanel x:Name="SortButtonPanel" Orientation="Horizontal" HorizontalAlignment="Center"/>
      <StackPanel MaxWidth="{x:Bind local:PersonList.itemsRepeaterWidth}" Orientation="Horizontal" Background="#EEE">
        <TextBlock Text="陣営" Width="50" TextAlignment="Center"/>
        <TextBlock Text="人物名" Width="80" TextAlignment="Center"/>
        <TextBlock Text="ロール" Width="50" TextAlignment="Center"/>
        <TextBlock Text="ランク" Width="50" TextAlignment="Center"/>
        <TextBlock Text="生年" Width="40" TextAlignment="Center"/>
        <TextBlock Text="登場" Width="40" TextAlignment="Center"/>
        <TextBlock Text="没年" Width="40" TextAlignment="Center"/>
        <TextBlock Text="列伝" Width="400" Padding="5,0,0,0" TextAlignment="Start"/>
      </StackPanel>
    </StackPanel>
    <ScrollViewer Grid.Row="1" x:Name="PersonScroll" MaxWidth="{x:Bind local:PersonList.itemsRepeaterWidth}">
      <ItemsRepeater x:Name="PersonItemsRepeater" ItemTemplate="{StaticResource PersonItemsRepeater}" Background="#CCC"/>
    </ScrollViewer>
  </Grid>
</UserControl>